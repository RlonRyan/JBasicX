apply plugin: 'java'

jar {
    baseName = 'JBasicX'
    version =  '0.1.0'
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
	resources {
            srcDir 'src'
        }
    }
}

// repositories {}

// dependencies {}

task wrapper(type: Wrapper) {
    gradleVersion = '2.2.1'
}